kibot:
  version: 1

preflight:
  run_erc: false
  run_drc: false
  set_text_variables:
    - name: date
      command: git log -1 --format='%as'
    - name: commit
      command: git log -1 --pretty=format:%h

global:
  output: "%f-%i.%x"

import:
  - generators/pcb-docs.kibot.yaml
  - generators/pcb-gerbers.kibot.yaml
  - generators/bom.kibot.yaml